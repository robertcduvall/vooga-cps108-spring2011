package vooga.gui.panes;

import vooga.gui.PaneManager;

/**
 * Type of popover that allows a user to input a value, and when 'accpt' button
 * is pressed (customizable look) returns the input to its parent.
 * @author Dave Crowe, David Colon-Smith
 *
 */
public class TextInputPane extends PopoverPane {

	public TextInputPane(PaneManager parent) {
		super(parent);
		// TODO -Stuff!
	}

}
